# Deprecated Starting Points

This folder is retained only as a transition marker.

Resume kickoff baselines now live in `../kickoff-resumes/`, and the approved reusable content model now lives in `../resume-data/`.

Do not add new starting points here.
